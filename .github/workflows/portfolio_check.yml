name: Portfolio Balance Check

on:
  # Run on every push to main
  push:
    branches: [ "main" ]
  
  # Run weekly on Sunday at noon Bangkok time (05:00 UTC = 12:00 Bangkok UTC+7)
  # Run monthly on the 1st at noon Bangkok time (05:00 UTC)
  schedule:
    - cron: '0 5 * * 0'   # Weekly: Sunday
    - cron: '0 5 1 * *'   # Monthly: 1st of month
  
  # Allow manual trigger
  workflow_dispatch:
    inputs:
      short_report:
        description: 'Short report (balance only, no trade history)'
        required: false
        type: boolean
        default: false
      monthly_report:
        description: 'Monthly report (full previous month trade history)'
        required: false
        type: boolean
        default: false

jobs:
  check_balance:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'
        cache: 'pip'

    - name: Install dependencies
      run: pip install --upgrade pip && pip install requests
      # Only install 'requests' - portfolio_balance.py doesn't need pandas/other heavy deps

    - name: Check Portfolio Balance
      env:
        # API credentials
        BITKUB_API_KEY: ${{ secrets.BITKUB_API_KEY }}
        BITKUB_API_SECRET: ${{ secrets.BITKUB_API_SECRET }}
        
        # Discord notification
        DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        
        # Configuration
        DCA_TARGET_MAP: ${{ vars.DCA_TARGET_MAP }}
        TIMEZONE: ${{ vars.TIMEZONE || 'Asia/Bangkok' }}
        
        # Report format: push=short (balance only), schedule/manual=full (with trade history)
        SHORT_REPORT: ${{ github.event_name == 'push' && 'true' || (github.event_name == 'workflow_dispatch' && inputs.short_report == true && 'true' || 'false') }}
        
        # Trade history window: monthly cron or manual monthly toggle = previous month, otherwise 7.5 days
        REPORT_DAYS: ${{ (github.event.schedule == '0 5 1 * *' || (github.event_name == 'workflow_dispatch' && inputs.monthly_report == true)) && 'previous_month' || '7.5' }}
        
      run: python portfolio_balance.py
